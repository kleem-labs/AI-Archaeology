# Exercises — Distance

1. Compute the Euclidean distance between `[0, 0]` and `[3, 4]`.
2. Compute the Manhattan distance for the same pair.
3. Find the nearest neighbor of `[2, 2]` among `[0, 0]`, `[2, 3]`, and `[5, 5]`.
4. Create a dataset where one large-scale feature overwhelms the rest.
5. Run `implementation.py` and compare the metrics.
