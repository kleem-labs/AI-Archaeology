# Diagrams — Vectors as Change

```mermaid
flowchart LR
    A[Start vector a] -->|add Δ| B[End vector b]
    B -->|subtract a| C[Change vector Δ]
    C -->|add to a| B
```

The change vector is independent of the path taken; it records the net displacement.
