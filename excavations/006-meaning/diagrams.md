# Diagrams — Meaning

```mermaid
flowchart LR
    C[Many sentences] --> W[Observe word neighborhoods]
    W --> P[Context patterns]
    P --> R[Relationships between words]
    R --> M[Evidence about meaning]
```

Meaning is inferred from repeated use, not stored as a single dictionary sentence.
