# Exercises — Embeddings

1. Why is a one-hot vector inefficient for a large vocabulary?
2. Compute cosine similarity for `[1, 0]` and `[1, 1]`.
3. Can two vectors have high cosine similarity but different lengths?
4. Why need embedding coordinates not have human-readable names?
5. Run `implementation.py` and add an embedding for *puppy*.

## Think

An embedding reflects its training data and objective. Describe two different datasets that could give the same word meaningfully different neighborhoods.
