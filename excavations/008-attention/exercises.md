# Exercises — Attention

1. In “Sam put the book on the table because it was sturdy,” what should *it* attend to?
2. Compute the weighted sum of `[1, 0]` and `[0, 2]` with weights `0.75` and `0.25`.
3. Why must attention weights be computed separately for each receiving token?
4. What information is lost if every token receives the same average?
5. Run `implementation.py` and change the query to favor a different token.
