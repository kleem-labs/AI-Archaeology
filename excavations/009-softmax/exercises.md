# Exercises — Softmax

1. Estimate softmax for `[0, 0]` without a calculator.
2. What happens when the same constant is added to every score?
3. Compare softmax of `[1, 2]` at temperatures `0.5`, `1`, and `2`.
4. Why is subtracting the maximum mathematically safe?
5. Run `implementation.py` with `[1000, 1001, 1002]`.
