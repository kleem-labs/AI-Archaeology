# Exercises — Query, Key, Value

1. Explain Q, K, and V using a library-search analogy.
2. Compute the dot products between query `[1, 0]` and keys `[1, 0]`, `[0, 1]`.
3. Why is a separate value projection useful?
4. What should a causal mask do to future attention weights?
5. Run `implementation.py` and inspect every row of its attention matrix.

## Build

Change one query without changing any keys or values. Explain why the attention distribution changes while the available information remains the same.
