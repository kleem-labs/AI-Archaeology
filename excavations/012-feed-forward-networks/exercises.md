# Exercises — Feed-Forward Networks

1. Apply ReLU to `[-3, 0, 2, 5]`.
2. Prove two linear layers without an activation collapse into one.
3. Trace all shapes for model width 4 and hidden width 12.
4. Run `implementation.py`; identify active hidden features.
5. Replace ReLU with `max(0.1*x, x)` and compare negative inputs.
