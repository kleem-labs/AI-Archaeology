# Exercises — Learning

1. Compute cross-entropy for correct-token probabilities 0.9, 0.5, and 0.01.
2. Differentiate `(w-3)²` and perform three updates from `w=0`.
3. Explain why the negative gradient is locally downhill.
4. Run `implementation.py` with learning rates `0.001`, `0.1`, and `1.1`.
5. Distinguish training loss, validation loss, memorization, and generalization.
