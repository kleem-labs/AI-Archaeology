"""Stage 2: the same Probability — Counting What We Do Not Know idea as array operations."""
import numpy as np

def inspect(values):
    array=np.asarray(values,dtype=float)
    return array,array.shape
