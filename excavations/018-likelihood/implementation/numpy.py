"""Stage 2: the same Likelihood — Which Hidden Story Produced This Evidence? idea as array operations."""
import numpy as np

def inspect(values):
    array=np.asarray(values,dtype=float)
    return array,array.shape
