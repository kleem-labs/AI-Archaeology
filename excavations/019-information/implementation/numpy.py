"""Stage 2: the same Information — Why Surprise Needs a Number idea as array operations."""
import numpy as np

def inspect(values):
    array=np.asarray(values,dtype=float)
    return array,array.shape
