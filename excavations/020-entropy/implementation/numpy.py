"""Stage 2: the same Entropy — Measuring the Uncertainty of a Whole Situation idea as array operations."""
import numpy as np

def inspect(values):
    array=np.asarray(values,dtype=float)
    return array,array.shape
