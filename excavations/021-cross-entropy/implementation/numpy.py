"""Stage 2: the same Cross-Entropy — Paying for Confidently Wrong Predictions idea as array operations."""
import numpy as np

def inspect(values):
    array=np.asarray(values,dtype=float)
    return array,array.shape
