"""Stage 2: the same Derivatives — Asking One Weight What It Changed idea as array operations."""
import numpy as np

def inspect(values):
    array=np.asarray(values,dtype=float)
    return array,array.shape
