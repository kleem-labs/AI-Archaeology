"""Stage 2: the same The Chain Rule — Following One Change Through Many Machines idea as array operations."""
import numpy as np

def inspect(values):
    array=np.asarray(values,dtype=float)
    return array,array.shape
