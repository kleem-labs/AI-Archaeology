"""Stage 2: the same Gradient Descent — Teaching a Tiny Network idea as array operations."""
import numpy as np

def inspect(values):
    array=np.asarray(values,dtype=float)
    return array,array.shape
