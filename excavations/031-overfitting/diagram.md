# Diagram — 031

~~~mermaid
flowchart LR
 A["Concrete problem"] --> B["Naive attempt"] --> C["Performance on remembered observations does not measure performance on unseen reality."] --> D["Reserve unseen cases and compare training success with performance outside the training memory."]
~~~

~~~text
observe -> attempt -> break it -> repair -> expose the next limit
~~~
