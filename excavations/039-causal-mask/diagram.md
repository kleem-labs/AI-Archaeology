# Diagram — 039

~~~mermaid
flowchart LR
 A[Concrete problem] --> B[Naive attempt] --> C[Visible failure] --> D["Process all positions together while blocking attention from position i to every later position j."]
~~~
