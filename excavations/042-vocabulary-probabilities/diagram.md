# Diagram — 042

~~~mermaid
flowchart LR
 A[Concrete problem] --> B[Naive attempt] --> C[Visible failure] --> D["Exponentiate relative scores, normalize them, then charge the negative log probability of the observed next token."]
~~~
