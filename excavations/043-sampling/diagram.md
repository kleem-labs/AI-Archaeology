# Diagram — 043

~~~mermaid
flowchart LR
 A[Concrete problem] --> B[Naive attempt] --> C[Visible failure] --> D["Control the distribution with temperature and optionally restrict it to a credible top set before sampling."]
~~~
