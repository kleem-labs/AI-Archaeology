# Visual brief — 062

Show the proposed action, missing boundary, visible failure, and repaired control path. Pause the animation before revealing the safeguard.

<!-- memory-film-v1:start -->
## Cinematic continuity sequence

Create one continuous five-frame scene—not five unrelated illustrations:

1. **Question:** What fails if we retry the action whenever a response is missing?
2. **Object:** the retries and idempotency gear mounted on the iron threshold
3. **Failure:** The gear follows the tempting path—retry the action whenever a response is missing. Then the evidence answers: the trouble appears immediately: the first payment succeeded and the retry charges the customer twice.
4. **Transformation:** The gatekeeper changes one moving part. The gear can now give each logical action a stable idempotency key, query existing state, and make repeated requests return the first result instead of repeating the effect.
5. **Seal:** Retries and Idempotency keeps the missing power: give each logical action a stable idempotency key, query existing state, and make repeated requests return the first result instead of repeating the effect.

Keep the same object, lighting, camera direction, and landmarks in every frame. The final frame may reveal notation faintly, but no equation may appear before the physical transformation has made every operation necessary.
<!-- memory-film-v1:end -->
