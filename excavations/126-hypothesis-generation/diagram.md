# Diagram — Hypotheses — Turning Curiosity into a Testable Claim

```mermaid
flowchart LR
    A["Observation"] --> B["Tempting shortcut"]
    B --> C["Counterexample"]
    C --> D["Required repair"]
    D --> E["Hypotheses"]
```

```text
observation -> attempt -> failure -> requirement -> discovery
```
