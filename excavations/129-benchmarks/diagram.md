# Diagram — Benchmarks — Building a Ruler Before Measuring Progress

```mermaid
flowchart LR
    A["Observation"] --> B["Tempting shortcut"]
    B --> C["Counterexample"]
    C --> D["Required repair"]
    D --> E["Benchmarks"]
```

```text
observation -> attempt -> failure -> requirement -> discovery
```
