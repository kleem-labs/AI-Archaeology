# Diagram — Long-Context Retrieval — Finding the One Clue That Matters

```mermaid
flowchart LR
    A["Observation"] --> B["Tempting shortcut"]
    B --> C["Counterexample"]
    C --> D["Required repair"]
    D --> E["Long-Context Retrieval"]
```

```text
observation -> attempt -> failure -> requirement -> discovery
```
