# Diagram — Reward Hacking — When the Score Replaces the Goal

```mermaid
flowchart LR
    A["Observation"] --> B["Tempting shortcut"]
    B --> C["Counterexample"]
    C --> D["Required repair"]
    D --> E["Reward Hacking"]
```

```text
observation -> attempt -> failure -> requirement -> discovery
```
