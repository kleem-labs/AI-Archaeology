# Diagram — Specification Gaming — Obeying the Words While Betraying the Purpose

```mermaid
flowchart LR
    A["Observation"] --> B["Tempting shortcut"]
    B --> C["Counterexample"]
    C --> D["Required repair"]
    D --> E["Specification Gaming"]
```

```text
observation -> attempt -> failure -> requirement -> discovery
```
