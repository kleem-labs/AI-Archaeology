# Invention Exercises — Excavation 153

1. Reconstruct the tempting design without using the chapter's accepted name: Load a batch, wait until loading finishes, compute it, and only then begin loading the next one.
2. Create the smallest measurement that reveals this failure: Data time and compute time are paid sequentially on every step, leaving expensive compute hardware idle.
3. Explain why the chosen arithmetic operation answers the job and why its nearest alternative does not.
4. Change one number in the worked run, predict every intermediate result, and only then run `implementation/pure_python.py`.
5. Invent a deployment where this limitation matters: Prefetching can hide latency, not unlimited data cost; workers, memory, or storage bandwidth can become the next limit.
