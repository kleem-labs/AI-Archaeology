# Invention Exercises — Excavation 159

1. Reconstruct the tempting design without using the chapter's accepted name: Return immediately to one KV head per query head.
2. Create the smallest measurement that reveals this failure: Quality recovers, but so does the full cache and bandwidth cost that forced sharing.
3. Explain why the chosen arithmetic operation answers the job and why its nearest alternative does not.
4. Change one number in the worked run, predict every intermediate result, and only then run `implementation/pure_python.py`.
5. Invent a deployment where this limitation matters: Because sharing deliberately removes independent KV views, the number and assignment of groups remain empirical design choices whose quality must be measured.
