# Invention Exercises — Excavation 170

1. Reconstruct the tempting design without using the chapter's accepted name: Reduce the batch until it fits and change nothing else.
2. Create the smallest measurement that reveals this failure: The gradient becomes noisier and the training regime changes; increasing the learning rate cannot recreate evidence never averaged together.
3. Explain why the chosen arithmetic operation answers the job and why its nearest alternative does not.
4. Change one number in the worked run, predict every intermediate result, and only then run `implementation/pure_python.py`.
5. Invent a deployment where this limitation matters: Accumulation lowers activation memory but adds serial work and does not reduce parameter or optimizer-state memory.
