# Invention Exercises — Excavation 172

1. Reconstruct the tempting design without using the chapter's accepted name: Add devices and replicate the full training state on each one.
2. Create the smallest measurement that reveals this failure: Compute capacity grows while per-device model-state memory remains almost unchanged, so the same memory wall returns.
3. Explain why the chosen arithmetic operation answers the job and why its nearest alternative does not.
4. Change one number in the worked run, predict every intermediate result, and only then run `implementation/pure_python.py`.
5. Invent a deployment where this limitation matters: Because a worker no longer owns a complete state by itself, sharding trades redundant memory for communication and makes recovery and state ownership more complex.
